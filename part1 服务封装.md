# 服务封装：真正搭建能够被访问的服务系统

- 能够将demo变成服务的核心就是构建客户端-服务端的通信。经过第一轮改造，本产品已具有初步完善的架构。本产品以服务器为核心，接收客户端的提问请求并进行相应的访问阿里云ai接口，访问数据库的操作。

## 核心部分：双端网络通信

### 网络通信原理
   
- 现代网络核心架构从上到下大致可分为应用层，传输层，网络层和链路层。每一个从客户端发来的请求都会被从上到下层层包装通过链路层传输，到服务端之后在从下到上进行拆包，解析指令并执行逻辑。
- 客户端发请求之前，服务端需要先对特定的端口进行监听。在服务器中多个端口可被一个进程监听，进程就是操作系统分配资源的基本单位。
```
uvicorn main_app:app --host 127.0.0.1 --port 8000 --reload
```
这条命令代表ip为127.0.0.1服务器创建了一个新进程，这个进程主动绑定8000号端口并进行监听，此时这个端口发来的请求就会被处理。现在就可以在客户端发送请求
```
curl -X POST http://127.0.0.1:8000/ask -H "Content-Type: application/json" -d "{\"question\": \"你们的退货政策是什么？\"}"
```
此时代表客户端向刚刚的ip地址和端口发送了请求。这个请求是基于应用层的http协议构造的传输报文。

- 此时请求就会被往下传。到传输层后，会由TCP协议接管。此时TCP协议会在封装的报文基础上增加源端口和目标的端口，并进行“三次握手”确认状态，综合以上信息添加tcp头。
- 到网络层后，数据包会被ip协议增加源ip地址和目标的ip地址以及TCP的协议字段。此时传输起始和终止的具体位置已经确定，可以进行传输了
- 到达链路层后就会开始真正的传输。由于127.0.0.1代表自机的ip地址，因此会通过特殊回路回传。假设是公网可访问的其他ip地址的服务器，链路层就会构建源MAC地址和目标MAC地址（通常为最近的路由器），通过路由器中转最后传向目标地址
- 数据包到达目标地址后，会通过链路层拆解出ip地址传回网络层，再由网络层传回协议字段以及端口号给传输层，再由传输层拆解TCP头并根据端口号处理http协议报文，最后交给应用层解析，处理。

### FastAPI后端框架
- FastAPI是python基于网络请求构建的web后端框架，可提供



- 
## 服务端逻辑：异步非阻塞
- 服务器在任务处理的结果层面上分为了同步，异步。同步是多个任务按顺序执行，只有返回结果之后才能执行下一个任务，期间服务器主动等待。异步就是任务开始后先挂起当前的任务，继续下一个任务，当有结果返回的时候再进行处理，这样可以在短时间内处理多个任务
- 服务器在处理任务在线程调度的方式上分为阻塞，非阻塞。阻塞就是等待期间线程被挂起不能执行其他任务，而非阻塞就是等待的时候线程不会挂起，交给事件循环
- 一个进程内会由多个线程。线程代表cpu进行资源调度的基本单位，相当于执行任务的基本单位，在传统多线程模式中，一个线程代表一个任务，一个进程内所有线程共享进程资源。
- 高并发任务通常指会频繁处理i/o事件（例如查询，请求接口等）的任务，通常这些任务耗时长会对服务器的事件的循环造成阻塞，同时多线程容易在大量请求的情况下发生资源竞争等一系列问题，因此需要在单线程内异步使用多个协程构建事件循环
- 一个线程中可以有多个协程，协程是纯用户端进行操作的，不涉及系统的调度。处理方式就是异步非阻塞，即在一个线程内，协程任务进入等待时会由事件循环决定执行其他协程，等待过程中线程不挂起。
```python
@app.post("/ask")
async def ask_question(req: AskRequest):   #异步函数声明
    logger.info(f"收到问题: {req.question}")    
    
    return StreamingResponse(
        generate_answer(req.question),    #这里需要加入异步逻辑
        media_type="text/plain; charset=utf-8" 
    )                                     #这一步先返回，等待内部逻辑异步执行完了返回结果。
```
- 之前提到的fastAPI的这个请求接口实际上就是异步处理方法的初步形成，体现了先返回，执行其他，等任务返回结果了再处理的逻辑。但是要让异步逻辑真正生效，需要在主任务处理函数，也就是 generate_answer(req.question)内部加入异步处理逻辑



